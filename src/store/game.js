import { defineStore } from 'pinia';

export const usegameStore = defineStore('game', {
    state: () => ({
        reappeareCode: '1234',
    }),
    actions: {
        
    },
    getters: {
        
    }
});